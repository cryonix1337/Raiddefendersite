[build]
  publish = "site/pages"
  functions = "netlify/functions"

[functions]
  node_bundler = "esbuild"

# Headers utiles pour les cookies de session
[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options = "DENY"
    Referrer-Policy = "strict-origin-when-cross-origin"
